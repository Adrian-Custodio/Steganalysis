# Base URL of the deployed stego-api backend (no trailing slash).
# Local dev (uvicorn default): http://localhost:8000
# Production: your live Render/Railway URL, set in Vercel's project env vars.
NEXT_PUBLIC_API_URL=http://localhost:8000
